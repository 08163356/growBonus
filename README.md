# GrowBonus 成长奖励系统

一个移动端优先的家庭教育激励系统，通过积分奖励正向行为、兑换奖品的方式，助力孩子成长。支持多角色协作（管理员、父母、孩子），让远程陪伴也能温暖有力。

## 功能特性

- **行为积分**：四大类行为模板（生活习惯 / 学习技能 / 社交品格 / 特别成就），一键录入，支持附带鼓励语和照片
- **奖品商城**：四档奖品（小奖 / 中奖 / 大奖 / 超级奖），支持物质奖励和体验奖励，兑换需大人审批
- **成就系统**：积分里程碑、连续打卡、分类达成等多维度成就
- **成长报告**：积分趋势、行为分类饼图、预算追踪
- **多角色**：管理员（全部权限）、家长（录入+查看）、孩子（互动+兑换）、访客（只读）
- **主题系统**：支持多套主题色 + 自定义背景图
- **PWA 支持**：可添加到手机桌面，离线可用

## 技术栈

| 层 | 技术 |
|---|------|
| 前端 | Vue 3 + TypeScript + Tailwind CSS 4 + ECharts + Lottie |
| 后端 | Express 5 + TypeScript + better-sqlite3 |
| 构建 | Vite 7 + vue-tsc |
| 部署 | Nginx + systemd |

## 项目结构

```
growBonus/
├── frontend/          # Vue 3 前端
│   ├── src/
│   │   ├── views/     # 页面（admin/ 大人端, child/ 孩子端）
│   │   ├── components/# 通用组件
│   │   ├── services/  # API 服务
│   │   ├── stores/    # Pinia 状态管理
│   │   ├── types/     # TypeScript 类型
│   │   └── utils/     # 工具函数和常量
│   └── vite.config.ts
├── backend/           # Express 后端
│   ├── src/
│   │   ├── database/  # SQLite 数据库（schema + DAO）
│   │   ├── routes/    # API 路由
│   │   ├── middleware/ # JWT 鉴权中间件
│   │   ├── scripts/   # 种子数据脚本
│   │   └── index.ts   # 入口
│   └── tsconfig.json
├── deploy/            # 部署配置
│   ├── install.sh     # 一键部署脚本
│   ├── nginx-growbonus.conf  # Nginx 配置片段
│   └── growbonus-backend.service  # systemd 服务
└── scripts/
    └── build-and-pack.ps1  # Windows 构建打包脚本
```

## 快速开始

### 环境要求

- Node.js >= 18
- npm >= 9

### 本地开发

```bash
# 1. 安装依赖
npm install

# 2. 配置后端环境变量
cp backend/.env.example backend/.env
# 编辑 backend/.env，设置 JWT_SECRET

# 3. 初始化数据库（首次）
npm run seed

# 4. 启动后端（终端1）
npm run dev:backend

# 5. 启动前端（终端2）
npm run dev:frontend

# 访问 http://localhost:5173/growbonus/
```

### 默认账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | uncle | 123456 |
| 父亲 | papa | 123456 |
| 母亲 | mama | 123456 |
| 孩子 | — | PIN: 1234 |

## 部署到服务器

### 1. 构建打包

```powershell
# Windows
powershell -File scripts/build-and-pack.ps1
```

产出 `growbonus-deploy.zip`。

### 2. 上传到服务器

```bash
scp growbonus-deploy.zip user@your-server:/tmp/
```

### 3. 服务器端部署

```bash
cd /tmp
unzip growbonus-deploy.zip -d growbonus-tmp
cd growbonus-tmp
sudo bash deploy/install.sh
```

脚本会自动：
- 部署到 `/var/www/growbonus/`
- 安装后端依赖
- 生成 `.env` 配置
- 初始化数据库
- 配置 systemd 服务并启动

### 4. 配置 Nginx

将 `deploy/nginx-growbonus.conf` 的内容添加到你的 Nginx server 块中：

```bash
sudo vim /etc/nginx/nginx.conf  # 或你的站点配置文件
# 在 server { ... } 块内粘贴 nginx-growbonus.conf 的内容
sudo nginx -t && sudo systemctl reload nginx
```

### 5. 验证

访问 `https://your-domain.com/growbonus/` 即可使用。

### 常用运维命令

```bash
# 查看服务状态
sudo systemctl status growbonus-backend

# 重启服务
sudo systemctl restart growbonus-backend

# 查看日志
sudo journalctl -u growbonus-backend -f

# 更新部署（重新上传 zip 后）
cd /tmp && sudo bash growbonus-tmp/deploy/install.sh
```

## 积分体系概览

| 类别 | 分值范围 | 示例 |
|------|---------|------|
| 生活习惯 | 2-5分 | 自己刷牙、按时睡觉、帮做家务 |
| 学习技能 | 5-100分 | 练琴打卡、背古诗、自主阅读 |
| 社交品格 | 3-8分 | 主动分享、用语言表达情绪 |
| 特别成就 | 50-300分 | 学完教材、累计背诵50首古诗 |

成长等级：🌱小种子 → 🌿小芽芽 → 🌳小树苗 → 🌲大树 → 🌸开花啦 → 🍎结果啦 → 👑小女王

## License

MIT
